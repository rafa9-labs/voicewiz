// Timer removed — component kept for future recording UI if needed.
// Re-adding the timer is as simple as uncommenting the return block below.
export default function RecordingHUD() {
  return null;
}
